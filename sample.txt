'epsilon'
node -> class
	id -> number
	to -> dictionary {term or nonterm : other node}
	final -> whether is a final node : returns id of a nonterminal if one exists, otherwise returns -1
	start -> ...
nonterminal -> class
	id -> number
	name
	first
	follow
	start_node
	final_node

dictionary {name : id}

